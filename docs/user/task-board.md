# 任务看板

在「设置 → 任务图」中，可以配置子任务的串行或并行执行方式。并行模式可设置 1 到 64 个子任务的最大并发数；串行模式固定为 1。开始后，看板按「待执行、执行中、待处理、已结束」显示任务状态，点击卡片可查看事件和可用操作。

「待处理」包括需要输入、审批或复核的任务，也包括失败后可重试的任务。可以按当前状态取消、继续、审核或重试；实际可用按钮以所选任务为准。看板状态来自服务端任务记录，刷新页面后仍可重新读取。

任务审核仅决定任务运行流程，不会自动合并 Git 变更。接收代码前请打开对应线程的差异和目标分支核验；任务显示「已完成」也不表示代码已合入目标分支。
