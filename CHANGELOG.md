# 更新日志

正式线与测试线（`-battle`）的全部对外版本变更都记录在这里。发布时，对应版本的小节会自动成为该版本 GitHub Release 的说明正文；应用内「设置 → 版本与下载」展示的更新日志也来自同一份数据。

- **正式线**：`X.Y.Z`，默认下载线，只包含经过验证的稳定版本。
- **测试线**：`X.Y.Z-battle`，先行体验新功能的预发布版本。安装测试线安装包后，应用会自动跟随测试线更新；要回到正式线，从「版本与下载」下载正式线安装包覆盖安装即可。

更早的历史版本见 [GitHub Releases](https://github.com/Sakana-yuyu/code-work/releases)。

## 1.0.27

### 新增

- Codex、Claude、Cursor、Grok、OpenCode 等 Agent 的流式正文、工具开始与结果统一进入对话时间线；工具失败保留可查看的记录，长任务的旧活动可展开回看。
- 网页、桌面和移动端的自定义模型设置支持按供应商选择渠道；余额页区分余额为零、供应商不提供查询接口和查询失败。

### 改进

- 本地账号池和共享网关在账号不可用时按模型限制选择其他健康账号；一次请求最多尝试四个不同账号，已经开始返回的响应流不会中途换号。
- 长对话减少重复计算与渲染；补充流式时间线、账号切换和多供应商路由的回归测试。

## 1.0.24

### 新增

- 同一对话可切换 Agent 并保留此前消息、回复与来源标记；切换后的 Agent 会收到前序对话和必要的工具结果摘要。多个 Agent 的活动在同一时间线中按发生顺序显示，工具调用与失败详情可展开查看。
- 新增本地 CLI 会话导入、双栏对话、任务看板与 Office 文件预览；项目内的会话引用和 Agent 交接入口更易找到。
- BYOK 网关支持经仅提供 Chat Completions 的中转服务转发 Responses 请求，并完善账号池转发路径。

### 改进

- 长对话的历史活动按需折叠和加载，保留正在执行的调用、错误及助手正文，降低重复渲染。
- 扩展通用 ACP Provider 目录与配置流程，补全相关用户文档和服务端回归测试。

## 1.0.22

### 新增

- BYOK 模型服务线程支持技能与 MCP 工具：输入 `/` 或 `$` 会列出可用技能（用户级 `~/.agents|claude|codex/skills` 与项目同名目录，含 `SKILL.md` 即为一个技能），选中的 `$技能名` 引用在发送前自动展开为完整指引，模型也可以主动调用 `skills.load` 按名加载；设置中已连接且信任的 MCP 服务器工具以 `mcp.<服务器>.<工具>` 并入模型工具清单，读取类全模式可用，写入与执行类仅在完全访问模式开放。

### 修复

- 修复 BYOK 中转通道把上游瞬时故障直接打死整个回合：网关此前对每个上游请求只发一次，中转站偶发的 5xx 或传输超时（如上游账号池瞬时 503）会原样透传并终止任务。现在响应流交付客户端前，对上游 5xx 与传输失败做有界重试（遵循 `retry-after`、封顶数秒）；429 限流仍连同响应头原样交回客户端自行退避。

## 1.0.21

### 修复

- 修复首条消息被误报为「已排队」且无法取消、无法编辑：线程详情订阅此前不推送排队的入队/取消事件，面板只能渲染陈旧的快照字段——首条消息在整个会话启动期间被误标为排队，取消与编辑看似没有反应。现在排队成员关系随事件流增量维护（入队、取消、回合采纳、会话终态清算），面板只在回合运行期间展示真正排队等待的消息；取消即时从面板消失，编辑在本机记录缺失时回退用服务端原文回填。
- 修复桌面端浏览器预览的元素点选浮层不跟随应用语言：浮层运行在隔离的 webview 预加载上下文，此前只能看到操作系统的区域设置，中文界面下仍显示英文。现在应用语言与注解主题经同一条通道下发到每个预览页面，浮层出现即为当前语言；拾取中途切换语言时，输入框占位、按钮与工具栏标签实时更新。

## 1.0.20

### 新增

- 空输入框按 ↑（ArrowUp）召回最近一条排队消息进入编辑：与队列行的「编辑」按钮同一条路径，对齐终端类工具的惯用交互；多行草稿中的光标上移不受影响。

### 修复

- 修复排队面板幻影条目：已分发的消息在订阅保活期间仍显示为「已排队」，直到整页重载才消失；现在条目所对应回合一旦起步即从面板移除，本地发件箱记录沿用同一口径。
- 侧边栏轨道指示条的颜色过渡此前挂在父元素上对伪元素不生效，现在过渡落在伪元素自身、悬停反馈真正可见。
- 浏览器表面滚动监听注册为 passive，不再阻塞滚动合成。

### 改进

- 服务端代码图/代码索引进度路径的平台判定改为显式注入：Windows 与 POSIX 行为在测试中确定性可断言，修复了 CI 在非 Windows 环境上的平台敏感失败。
- 传输预算集成测试的解码上限按流式批处理的真实方差校准，保留对大体量载荷回归的拦截能力。

## 1.0.19

### 新增

- 排队消息支持一键清空：排队两条及以上时，面板头部出现「清空全部」，逐条取消且不影响正在分发的消息；逐条编辑、取消、引导行为不变。

### 修复

- 自定义模型服务的「思考强度」选择器现在对所有渠道模型可用：此前只有内置目录收录的模型显示该选项，手动添加或自动发现的目录外模型一律没有；现在未收录模型提供保守档位（关闭/低/中/高），不点选则不向端点发送该参数，保持供应商原生默认。
- 修复 Claude 代理下选用自定义模型渠道时思考强度「能选但不生效」：运行时用静态目录校验选项，目录外模型的选择被静默丢弃；现在枚举内的显式选择会透传进请求，Claude 走桥接的 OpenAI 兼容渠道也会把强度映射为上游的 reasoning_effort。「关闭」等 Claude 不支持的取值不会发送；内置目录中本就不支持思考的模型（如 Haiku）行为不变。
- BYOK 桥接三处健壮性修正：上游同时回传两种用量字段拼写时不再重复计数 token；只含思考内容的助手回合桥接后不再产生会被严格端点拒绝的空消息；count_tokens 估算现在计入工具结果内容，预估不再严重偏低。

## 1.0.18

### 修复

- Codex 线程的回合「模型用时」一直为空：Codex 在回合完成事件里上报了耗时，但适配器没有接入规范载荷。现在透传该字段并入回合用量快照，会话统计弹层可以显示真实耗时；其他 provider 不受影响。
- 在网页端/桌面端排队的消息，移动端会话页现在也会计入排队数量——此前只数本机发件箱，跨端排的队在手机上完全不可见。
- 工作区源码清理：会话投影源码中两处字面 NUL 分隔符改为转义写法；服务端与 Web 的 Effect 类型诊断全部清零，`tsgo --noEmit` 恢复零输出、零退出码的门禁状态。

### 改进

- Spec Workflow 标记按钮的说明文字改用统一 Tooltip 浮层展示，替代浏览器原生 title。

## 1.0.17

### 新增

- 用量页新增「自定义模型服务」来源：BYOK 线程每次模型响应的 token 用量（含缓存命中与思考 token 拆分）现在会进入用量统计，与 Codex、Claude Code、Grok Build 并列出现在供应商占比、趋势图与模型明细中。用量记录保存在本机服务数据目录，委派给外部 CLI 的子任务仍按各自 CLI 的会话记录统计。
- 「路由走自定义模型渠道」的 Claude 实例可以选用 OpenAI 兼容渠道：BYOK 网关新增 Anthropic Messages ↔ Chat Completions 双向桥接，请求、响应与流式输出自动翻译，Claude 的可选模型从仅 Anthropic 协议渠道扩展到全部已启用渠道；Codex 等 OpenAI 路由维持原有同协议直通，Anthropic 协议渠道暂不对 OpenAI 路由反向开放。

### 改进

- 长回合的工作日志折叠行不再是干巴巴的「+337 次更早工具调用」：折叠起来的几百次调用现在会附上构成摘要（如「已读取 142 个文件、已运行 96 条命令」），一眼能看出这段时间做了什么；Web 与移动端一致。
- 运行中发送的排队消息改为服务端队列：一次可排多条、逐条取消，刷新或重新打开后队列仍在——此前队列只存在页面内存，刷新即丢且取消/编辑按钮全部失效。取消只影响尚未开始的回合，已被 agent 采纳的消息不受影响；移动端沿用本地发件箱，行为不变。

### 修复

- 修复 agent 回复互相覆盖、个别情况下串写到其他线程的严重问题：部分 provider 事件的 item ID 会跨回合甚至跨线程重复（实测同一 `item_0` 被反复复用），旧实现把它当作全局唯一消息标识，后到的回复会改写先到回复的正文。现在消息标识由线程、回合与 item 共同生成，各回合回复彼此独立；该修复只影响更新后新写入的事件，历史已被覆盖的数据不会自动还原。
- 自定义模型服务路由出来的模型恢复「思考强度」入口：网关此前给所有路由模型上报空能力描述，输入框只剩权限选项；现在优先沿用原模型的能力，OpenAI 协议路由缺失时按模型 ID 查询内置目录，未知模型不虚构能力。
- 上下文窗口的缓存命中率修正统计口径：此前可能把累计输入 token 与单轮缓存 token 混除导致读数失真，现在优先使用同口径的累计对，缺累计数据时回退到单轮对；没有缓存数据时保持未知而不是硬算。
- BYOK 分组名恰好是模型 ID 前缀时，模型名不再被错误截断：分组「GLM」下的 `glm-5.3`、分组「Grok」下的 `grok-4.5` 曾被显示成「5.3」「4.5」，现在只有明确的标签分隔符（`:`、`·`、`/`、`|`）才触发前缀剥离。
- 失败回合的错误行不再被折叠进「+N 条更早日志」：回合失败的原因直接可见，不需要点开折叠层排查。
- 切换到 IDE 模式时左侧活动条首帧显示为深色黑条：工作台把活动条、侧栏、编辑区的底色写成内联样式，而主题同步要等扩展激活完才启动，落地前用的是引导主题的深色底。现在这些区域首帧就由应用侧配色绘制，主题同步后的颜色与此前完全一致。
- 集成页 CodeGraph「建立索引」在未建过索引的项目上必然失败（报 `[ERR] CodeGraph not initialized`）：手动建立误用了只对已初始化项目生效的全量重建命令，现已按项目状态选择命令——未初始化走 `codegraph init`（与 agent 回合自动建索引同一条命令），已初始化才走 `index` 重建；后台自动建索引在飞时不重复拉起第二个构建进程。同时修正自动建索引拉起的命令行：上一版引入的 `--yes` 选项在已发布的 codegraph CLI（1.5.0）上不存在，导致首次索引从未真正建成。
- 工作日志出现只有「✓ Account rate limits updated」一行、没有任何内容的伪工具行：订阅额度刷新是「最新态」记录，由输入框的额度芯片与用量页呈现，不再当作工作日志条目显示（Web 与移动端一致）。
- 自定义模型服务里 `deepseek-flash` 等不带版本号的 DeepSeek 中转别名显示 64000 tokens / 最大输出 4096 的 V2 时代旧规格：目录已按 DeepSeek 官方 2026-09 阵容核对——`deepseek-flash` 是官方现役模型 ID（DeepSeek V4.1-Flash，100 万上下文 / 384K 最大输出 / 支持视觉，旧名 deepseek-v4-flash 已并入该模型，同样获得视觉与 384K 输出），`deepseek-v4-pro` 与 V4 系列最大输出按官方修正为 384K，未知 DeepSeek 型号的兜底也从 64K/4K 抬到 V3 时代起的下限 128K/8K。已有模型行的存储值不会被自动抬升（目录只收敛过大值），在编辑弹窗里重填一次模型 ID 触发目录回填即可刷新。

## 1.0.16

### 新增

- 输入框「+」菜单新增「子代理模式」：自定义模型服务线程开启委派引擎，codex 与 claude 开启后注入提示词指引，引导 agent 用原生子代理（Task/collab agents）并行处理可独立的子任务；选择后自动生效，输入框下方出现常驻胶囊标识，可直接在胶囊上关闭，无需再进菜单。
- 代码索引（设置 → 集成 → 代码索引）实时进度：建立或更新索引时逐项目显示「正在扫描文件…」与「已索引 X / Y 个文件」，索引进行中面板自动刷新；大项目首次建索引不再阻塞其他项目的状态显示。
- 多任务队列支持「引导」：回合运行中点击排队消息上的引导按钮，消息立即发送给 agent 但不打断当前输出；agent 在下一轮读取后结合当前进展自行判断、调整方向。引导带图片附件的消息不支持；当前仅自定义模型服务（BYOK）线程提供该按钮。
- 集成开源 CodeGraph 代码知识图谱（默认开启，设置 → 集成 → CodeGraph 代码图谱）：安装 [codegraph CLI](https://github.com/colbymchenry/codegraph) 后，项目会在首个 agent 回合后台自动建立索引，agent 获得 `codegraph.explore` 结构化查询工具与使用策略——跨文件理解、调用关系、数据流、架构分析、Bug 根因定位、重构影响面等关系型问题优先走图谱，已知位置的简单修改与单文件任务照常用常规工具，避免无意义调用撑大上下文。索引全程本地；未安装 CLI 或关闭开关时 agent 不受影响，已建索引永不自动删除。集成页同时升级为运维面板：CLI 缺失时支持一键安装（npm 全局装最新版）并显示探测版本；逐项目展示索引健康卡（文件/节点/边统计、语言、索引体积、最近索引时间、待同步变更数、旧版索引「建议重建」提示）；后台建索引/重建实时显示扫描 → 解析 → 引用 → 链接各阶段（上游只报阶段不报百分比，故不做假进度）；每个项目可直接同步或重建索引。详见 `docs/user/codegraph.md`。
- 规格驱动开发对齐上游 kamioj/spec-workflow：新增跨任务复用的知识库（`spec/knowledge.md` 索引 + `spec/knowledge/` 子文档，调研/实施/修复/验证先行读索引、已录结论直接信任，调研与归档沉淀经验）；归档前强制产出 `retrospect.md` 复盘（偏差、证据、遗留项），归档时自动把 `spec/changes/<name>/` 移入 `spec/archive/<日期-name>/`；独立验证台账升级为稳定编号+代码证据规则（引用不出代码的发现自动撤销，未修复发现判定失败）；节点选择器新增可叠加的「设计模式」（视觉关键任务按前端美学执行）与「严格模式」（反偷懒反臆测，结论必须可引用证据）执行取向开关。

### 修复

- 集成页 CodeGraph 的「同步索引」「重建索引」在从未建过索引的项目上永远置灰，导致无法从面板手动建立首个索引：重建按钮现在只要求 CLI 已安装即可点击，未建索引时显示为「建立索引」；首次全量重建的超时上限从 5 分钟放宽到 30 分钟。
- agent 运行失败后编辑重发，旧回合只剩工具记账时遗留的空「工作了 Xs」折叠行不再显示。
- 修复回合耗时显示缺少小时与天的问题（超过 1 小时显示为 `1h 2m`，超过 1 天显示为 `1d 3h`）。

### 改进

- 只读工具（读文件、搜索、目录列表等）并发执行，减少长回合中串行等待；模型单轮内一次性给出多个只读调用时不再逐个排队。
- codex 等输出密集的模型运行时，回合内已定稿的叙述文字自动折叠为「+N 段更早输出」，实时视图只保留最新一段叙述与工具活动，可点击展开回看，不再堆成整面文字墙。
- 集成页说明如实区分两个互补能力：「代码索引」回答符号定义在哪，「CodeGraph 代码图谱」回答代码之间如何关联；自动建索引的时机明确为自定义模型服务的 agent 回合。
- 命令行入口与界面文案统一为 codework，移除残留的 T3 字样；npm 包名、域名、协议常量等对外契约保持不变。

## 1.0.14

### 修复

- 修复 BYOK 长回合下缓存命中率极低的问题（实测仅三到五成）：主线程 agent loop 不再按固定条数逐轮滑动上下文，改为在模型上下文窗口约 80% 以内全量重放，请求前缀跨轮稳定、prompt cache 持续命中，仅在逼近窗口上限时整体裁剪一次；委派子代理维持原有滑窗行为。

## 1.0.13

### 新增

- 会话统计改为输入框下方的双胶囊：轮次/步数与总 token/缓存命中率一目了然，点击可查看模型用时、工具调用用时和 token 明细；步数改按真实模型响应计数。
- 排队消息面板外置为输入框上方的独立条带（与下方上下文条呼应），支持点击编辑：内容回到输入框，修改后可直接再次排队。
- 排队中的消息在时间线内不再以卡片形式重复展示。

### 改进

- 思考摘要相邻片段聚合为一条可展开分组，回合结束后随「工作了」折叠收纳，长回合不再刷屏。
- 停止生成按钮增加中断中状态反馈。

## 1.0.12

### 修复

- 修复目标已由服务端完成清理后，客户端仍显示目标且清理操作误报失败的问题。
- 修复点击继续时重复发送完整目标内容的问题，改为使用目标上下文继续执行。
- 修复长任务完成后思考摘要重复堆积，并补齐 BYOK 的缓存 token、模型耗时和轮次统计。

## 1.0.11

### 修复

- 放宽 BYOK Agent Loop 的模型调用轮次，不再因固定 64 轮上限提前停止；仅在模型流断流且尚未产生输出时重连，最多轮询 10 次。

## 1.0.10

### 修复

- 允许 SSH 原生依赖在 Electron 跨平台重建前生成构建探测文件，修复 Linux 与 macOS 发布构建失败。

## 1.0.9

### 修复

- 修复 SSH `ssh2` 原生依赖未被桌面 sidecar 外置，导致跨平台构建解析 `cpufeatures.node` 失败的问题。
- 修复 SSH 面板使用原生 `title` 提示触发 Web lint 失败的问题。
- 按 Provider 分段边界事件更新传输预算回归阈值，保持线传输和消息数量上限不变。

## 1.0.8

### 新增

- 新增远程 SSH 服务器管理：可在设置中登记主机、测试连接、查看状态，并在右侧面板打开 SSH 终端或浏览远程文件。
- 编排智能体新增 SSH 状态、命令执行、文件浏览、读取、写入和删除工具；连接、主机指纹、路径和删除范围由服务端统一校验。
- Canvas 新增 callout、todo、代码、徽章、用量条、柱状图、折线图、饼图、diff 和折叠说明等结构化区块，并在右侧面板中展示。
- 模型选择器支持按供应商分组，并可将收藏模型集中到独立分组。

### 改进

- BYOK 工具调用按能力分组，工具输入 schema 与服务端契约保持一致；模型把 Canvas JSON 写进正文而未调用工具时，可在满足条件时自动恢复为 Canvas 交付。
- 对话时间线、移动端线程活动和工具调用状态继续补齐跨端事件展示，减少长任务中的状态丢失。
- Spec Workflow 调度失败不会再被错误缓存为已处理，后续相同意图可以重试。

### 修复

- 修复 SSH 工具注册表、ToolBroker 实现和测试断言不一致导致的工具能力误报。
- 收敛 Canvas、SSH 服务和工具调用中的冗余代码，避免未接入契约继续暴露为公共接口。

## 1.0.7

### 新增

- 源代码管理新增 AI 生成提交信息：按当前变更一键生成符合规范的提交说明，跟随界面语言（简体中文 / English / 日本語）输出；生成的信息只填入输入框，不会自动提交。内置 VSCode 工作台的 SCM 输入框按钮同步接入。
- 对话时间线新增「思考摘要」：模型的思考过程以可折叠的摘要行展示，原始思维链不会进入对话记录。
- 对话新增统计条：展示轮次、步骤、模型与工具耗时、缓存命中率以及输入输出 token 用量。
- 编排智能体新增三个只读工作区工具（列出文件、按名称搜索、按内容搜索），agent 可以在不写文件的前提下自助探查项目结构。
- 自定义模型服务支持思考强度档位：按内置目录为支持的模型提供档位选择，并随请求发送对应参数。
- Kimi 支持经自定义模型服务的共享线路接入。

### 改进

- 大幅提升「用量」页的打开速度：查询分层加载、文件解析单飞与结果缓存，token 统计不再久等不出。
- 适配各模型供应商的思考链行为：DeepSeek、GLM、MiniMax、Anthropic 等在工具调用后正确回传思考内容，修复「thinking 模式必须回传 reasoning_content」一类 400 报错；兼容 OpenRouter 的思考字段。
- 自定义模型服务的会话跨重启保留：服务器重启或升级后继续对话不再丢失上下文。
- 单个对话内锁定 agent 切换：对话已开始后切换 agent 或跨实例模型会给出明确提示，避免上下文不通导致反复造轮子；移动端同步生效。
- IDE 布局的右侧面板（Agent / Diff / 预览等）改为覆盖层展开，面板入口在工作台模式下不再失灵；「对话 / IDE」布局切换按钮常驻可达。
- 手动添加模型时按内置目录自动回填上下文窗口与最大输出（仅回填未改动过的字段）。

### 修复

- 修复数据库迁移水位线越过旧编号后部分迁移被永久跳过的问题（迁移 065/066 重编号为 078/079 并幂等化，已有库可安全重放）。
- 修复画布产物目录被 gitignore 时，提交暂存与检查点捕获直接失败的问题。
- 修复 Windows 下 node-pty 在进程列表缺失时未处理异常导致宿主进程崩溃的问题。
- 修复 Codex CLI 因 npm 平台包缺失而损坏时仍显示为可用的问题，现在会提示一键修复。
- 修复 IDE 布局下面板浮层无法关闭、布局切换按钮被遮挡导致无法切回的问题。

## 1.0.6

### 新增

- 余额看板支持智谱 GLM / Z.ai：`open.bigmodel.cn` 与 `api.z.ai` 域名下的通道会通过智谱官方接口读取 GLM Coding Plan 的套餐档位、配额窗口（滚动窗口 / 每周窗口、已用百分比与下次重置时间）和钱包余额（账户报表接口，不可用时回退 v4 balance 接口）。

### 改进

- 内置模型能力目录新增 2026 年 9 月发布的模型（按官方发布信息核对上下文窗口、最大输出与视觉能力）：Claude Fable 5.1 / Mythos 5.1（1M 上下文 / 128K 输出）、GPT-6 Astra（1.05M / 128K）、Gemini 3.8 Flash（1M / 64K）、DeepSeek V4.1-Flash（1M / 384K）、Grok 4.6（500K / 32K）、Qwen3.8-Max（1M / 128K），这些模型此前会被通用兜底规则按错误数值处理。
- 目录新增模型支持的思考强度档位数据（如 GPT-6 的 low/medium/high/xhigh/max、Gemini 3.8 与 Claude Fable 5.1 的 low/medium/high），供后续能力展示与请求参数使用。

## 1.0.5

### 新增

- 自定义模型服务的模型编辑新增「自定义请求头（高级）」：填写 JSON 对象（如 `{"X-Custom":"value"}`）即可为发往该中转的所有请求附加额外请求头，可覆盖协议默认头；头的值按密钥存储，不会回显到任何客户端，需要移除时勾选“清除已存储的自定义请求头”。
- 中转详情新增「编辑通道」：一次修改分组名称、协议、服务地址、API 密钥、自定义请求头和余额设置，保存后应用到该通道下的全部模型，不需要逐个模型重复修改。
- 中转详情新增「删除通道」：一次性移除该通道下的全部模型及已存储的密钥。
- 设置的自定义模型服务新增「一键优化」：一次对齐全部通道的上下文窗口，并按内置模型目录回填模型的最大输出上限，同时列出协议配置问题。

### 改进

- 添加或导入模型时按模型名称自动推断请求协议：Claude 模型走 Anthropic 原生协议、Gemini 模型走 Gemini 原生协议，避免走 OpenAI 兼容协议导致前缀缓存失效。
- 内置模型能力目录同步扩充（含 GLM-5.3 系列），并为每个模型补充官方最大输出上限：请求的输出上限按该值生效，Claude Sonnet 4.6 等模型不再被默认的 8,192 上限截断。
- 「发现模型」读取到的模型会直接用内置目录补全上下文窗口与最大输出，新添加的模型从第一天起就带正确的元数据，而不是落到 128K 默认值。
- 视觉委派按内置目录核实的图片输入能力判断模型是否直传图片，减少把图片发给纯文本模型、或把支持视觉的模型错误送去识图的情况。

### 修复

- 修复从 cursor-byok 导出文件（YAML）导入模型时，导入的 API 密钥被直接丢弃、实际无法使用的问题；导入时已启用的自定义请求头也会一并迁入。

## 1.0.5-battle

与 1.0.4 内容一致，用于接续测试下载线：测试线安装（1.0.1-battle 及更早）可直接升级到本版本。

## 1.0.4

### 新增

- 设置新增「版本与下载」页面：浏览正式 / 测试两条发布线的全部版本，查看每个版本的更新日志，并直接下载各平台安装包（Windows、macOS Apple 芯片 / Intel、Linux）。
- 仓库根目录新增 `CHANGELOG.md`，发布时对应小节自动成为 GitHub Release 说明。

### 改进

- 手机端模型选择器按驱动显示供应商名称（Pi、OhMyPi、ACP Agent 等），新供应商分组默认展开。

## 1.0.3

### 新增

- 新增 Pi、OhMyPi（OMP）、ACP Agent 三类 Agent 供应商：模型调用全部强制经「自定义模型服务」（BYOK）路由，不回退、不使用供应商自带凭据；工具调用完整接入权限审批生命周期。

### 修复

- 测试版桌面应用可直接升级到下一个测试版。
- IDE 模式切换的引导重试、主题扩展注册与预览显示。
- 编队（Squad）保存不再丢失团队默认模型绑定。

### 安全

- BYOK 配置脱敏递归化，日志与事件中不再出现密钥字段。

## 1.0.1-battle

### 修复

- 安装器钩子不再自定义 WinMessages 常量，避免与 MUI2 冲突。
- 背景装饰下终端与侧栏分区头保持透明。
- 桌面 CSP 放行 `data:` 清单读取，修复 IDE 主题同步静默卡死。

### 改进

- 安装目录选择盘符根目录后自动补全应用子目录。
- 主题列表卡片显示背景图缩略图。
- IDE 项目选择页改为卡片布局并加品牌水印。

## 1.0.0-battle

### 新增

- 内置工作区 IDE：页内 VSCode 工作台，支持主题同步、语言服务与扩展。
- battle 测试发布线：预发布版本走独立更新通道。
